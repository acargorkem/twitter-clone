{
  "name": "twitter-clone",
  "version": "0.1.0",
  "description": "Full stack twitter clone",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
    "postinstall": "cd backend && npm install && cd.. & cd frontend && npm install && cd .."
  },
  "author": "Görkem Acar & Levent Önder",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
